class Board
  def build_table
    #make a 2d arr with coords as subarr and accompany
    #it with a hash table (coords as keys) with all the values
  end

  def render
    #print display
    #take values, make a str, and p each line individually
  end

  def match_count(coord)
    #display.flatten.count { |i| i == hash[key] }
  end

  def [](x, y)
    #coord arr
  end

  def []=(x,y,z)

  end
end
