class Card

  def check
    
  end
  def reveal(coord)
    #take coord = hash[coord] to show 1
    #take coord = hash[coord] to show 2
    # then coord = coord again to hide if not match_count == 2
  end

  def hide(coord)
    #change back
  end
end
